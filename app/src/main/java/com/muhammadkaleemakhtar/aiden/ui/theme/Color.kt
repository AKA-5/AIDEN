package com.muhammadkaleemakhtar.aiden.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Strict color palette for the AIDEN application.
 * Contains no blues or neon colors, as specified in the MASTER_PLAN.
 */

// Primary Action Color
val Terracotta = Color(0xFFE76F51)

// Secondary Accent Color
val WarmSand = Color(0xFFF4A261)

// Main Background Color (Warm White)
val WarmWhite = Color(0xFFFDF8F5)

// Surface Background Color (Pure White)
val PureWhite = Color(0xFFFFFFFF)

// Primary Text Color (Dark Brown)
val DarkBrown = Color(0xFF2C1810)

// Secondary/Label Text Color (Muted Brown)
val MutedBrown = Color(0xFF7F5E4A)

// Success Indicator (Teal Green)
val TealGreen = Color(0xFF2A9D8F)

// Warning Indicator (Mustard)
val Mustard = Color(0xFFE9C46A)

// Crisis/Error Indicator (Coral Red)
val CoralRed = Color(0xFFE63946)

// Separators and Borders (Warm Gray)
val WarmGray = Color(0xFFE8DCD4)